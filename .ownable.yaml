# .ownable.yaml — content layout for the Ownable Markdown Editor.
# https://ownable.dev/docs/ownable-yaml
version: 1
types:
  blog:
    dirs:
      - blog
    bundle: false
  event:
    dirs:
      - events
    bundle: false
  project:
    dirs:
      - projects
    bundle: false
  slides:
    dirs:
      - slides
    bundle: false
